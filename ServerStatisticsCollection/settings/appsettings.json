{
  "RabbitMqSettings": {
    "Hostname": "localhost",
    "UserName": "guest",
    "Password": "guest"
  },

  "ServerStatisticsConfig": {
    "SamplingIntervalSeconds": 60,
    "ServerIdentifier": "windows1"
  }
}